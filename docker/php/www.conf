; Matrix SSE holds a worker up to ~120s; keep above nginx fastcgi_read_timeout for PHP routes.
; Override php docker.conf access.log (/proc/self/fd/2) — that flood is what Grafana
; showed as "nginx access". Real request access logs come from nginx instead.
[www]
request_terminate_timeout = 3600s
access.log = /dev/null
